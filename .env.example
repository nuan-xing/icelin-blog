# The deployed browser configuration lives in public/api-config.js.
# Keep only public URLs there. Never put R2 credentials, Worker passwords,
# API tokens, or ADMIN_SESSION_SECRET in this file or in Pages variables.

# Cloudflare Worker secrets are set interactively:
# npx wrangler secret put ADMIN_PASSWORD
# npx wrangler secret put ADMIN_SESSION_SECRET
